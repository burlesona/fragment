// Generated by CoffeeScript 1.7.1
(function() {
  var frTpl, root, usTpl;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  usTpl = "<div class=\"content\">\n  <h1 data-test=\"example\">Test Content</h1>\n  <p><%= paraMessage %></p>\n  <ul>\n    <li><%= li1 %></li>\n    <li><%= li2 %></li>\n  </ul>\n</div>";

  frTpl = ".content\n  h1{\"data-test\": \"example\"} Test Content\n  p = @paraMessage\n  ul\n    li = @li1\n    li = @li2";

  root.init = function() {
    var clear, counter, run;
    counter = document.getElementById('count');
    run = document.getElementById('run');
    clear = document.getElementById('clear');
    run.addEventListener('click', function() {
      return performanceTest(parseInt(counter.value));
    });
    return clear.addEventListener('click', function() {
      return document.getElementById('fixture').innerHTML = "";
    });
  };

  root.performanceTest = function(iterations) {
    var i, output, temp, _i, _j;
    console.time('underscore');
    for (i = _i = 0; 0 <= iterations ? _i < iterations : _i > iterations; i = 0 <= iterations ? ++_i : --_i) {
      output = _.template(usTpl, {
        paraMessage: "I'm a paragraph with some content inside.",
        li1: "First List Item!",
        li2: "Second List Item!"
      });
      temp = document.createElement('div');
      temp.innerHTML = output;
      document.getElementById('fixture').appendChild(temp.firstChild);
    }
    console.timeEnd('underscore');
    console.time('fragment');
    for (i = _j = 0; 0 <= iterations ? _j < iterations : _j > iterations; i = 0 <= iterations ? ++_j : --_j) {
      output = Fragment.parse(frTpl, {
        paraMessage: "I'm a paragraph with some content inside.",
        li1: "First List Item!",
        li2: "Second List Item!"
      });
      document.getElementById('fixture').appendChild(output);
    }
    return console.timeEnd('fragment');
  };

}).call(this);
